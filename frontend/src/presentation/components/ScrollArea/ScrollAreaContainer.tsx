import { ScrollArea } from "./ScrollArea"
import type { ScrollAreaProps } from "./ScrollAreaConfig"

function ScrollAreaContainer(props: ScrollAreaProps) {
  return <ScrollArea {...props} />
}

export { ScrollAreaContainer }
