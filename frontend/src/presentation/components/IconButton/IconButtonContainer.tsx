import { IconButton } from "./IconButton"
import type { IconButtonProps } from "./IconButtonConfig"

function IconButtonContainer(props: IconButtonProps) {
  return <IconButton {...props} />
}

export { IconButtonContainer }
