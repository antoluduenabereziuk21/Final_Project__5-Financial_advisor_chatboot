import { Popover } from "./Popover"
import type { PopoverProps } from "./PopoverConfig"

function PopoverContainer(props: PopoverProps) {
  return <Popover {...props} />
}

export { PopoverContainer }
